{
    "name":"terminal", 
    "title": "Terminal App",
    "command_list": ["export PATH=${PREFIX}/bin:${PATH}; alias conda=/opt/anaconda/bin/conda; ${PREFIX}/bin/tty.js", "--url_prefix=%(urlPrefix)s", "--cwd=%(projectHome)s", "--port=%(port)s"]
}

